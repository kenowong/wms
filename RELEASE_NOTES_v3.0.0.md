# 进销存管理系统 v3.0.0 更新说明

> 更新日期：2026-09-15　｜　适用：Windows 桌面安装版 / NAS-Docker 自部署 / 手机网页端

---

## 一、新增：介绍方提成（佣金）

**给"靠人介绍成交"的单子记一笔佣金，利润不再虚高。**

- **往来单位可标记为介绍方**：档案里勾选「是介绍方」，开单时下拉里直接选得到。
- **销售单直接登记提成**：新增「介绍方 / 提成总额 / 提成税点」三项。税点提供 **4% / 5% / 10%** 快捷按钮，也可手填任意点数。
- **自动拆分代扣税与实付**：录入谈定 2000、税点 4%，立即显示「代扣税 80、实付介绍方 1920」，不用自己算。
- **出库后也能补录**：单子先开出去、提成事后才谈定，销售单详情里点「登记提成」随时补，金额也能改。
- **提成计入成本，毛利不虚高**：提成按**扣点后的实付金额**冲减成本，工作台、项目成本、数据报表里的利润全部自动扣除。举例：进价 1000、卖 5000、提成 2000（税点 4%）——不扣提成显示毛利 3539.82，实际正确值是 **1619.82**。
- **财务侧独立台账**：财务管理新增「介绍提成」页，按单列出介绍方、提成总额、税点、代扣税、应付实付、已付、未付。
- **分次支付**：提成可以分几次付（先付一半也行），选付款账户自动扣银行余额并生成流水。
- **撤销支付**：付错了可以撤销，金额自动退回账户。
- **支付流水**：每笔提成的支付记录可单独查看。

## 二、新增：期初建账

**老账套搬进系统，从这一刻开始账才是对的。**

- **五类期初一次录完**：库存期初、应收应付期初、银行期初、现金期初、固定资产期初。
- **一键「应用期初」**：应用后库存写入库存台账、银行余额直接设定、现金自动生成独立现金账户。
- **期初欠款自动并入往来**：「待收 / 待付单据」里自动带出「期初欠款」行，收付款时可直接核销。
- **财务数据自动叠加**：财务汇总的应收 / 应付、对账单余额，都自动加上期初数，不需要手工调。
- **支持重置**：期初录错了可以重置后重新应用；如果已经产生了业务单据，系统会自动拦截，防止把账冲乱。

## 三、改进

- **商品明细列宽可拖拽并记住**：采购单、销售单明细里的「规格」「型号」等列，拖表头右边缘即可加宽，宽度记在本地，下次开单保持同样宽度。
- **盘点单支持删除**：误建的盘点单可以直接删掉，不用留着占位。
- **手机端连接页**：新增 `/launcher` 页面，手机浏览器打开后可填写服务器地址并进入系统，也可"添加到主屏幕"当作 App 使用。
- **发票打印补回「发票号码」字段**：打印模板里可以直接引用发票号码。
- **银行账户区分类型**：银行账户与现金账户分开管理，期初建账和收付款选账户更清晰。

## 四、修复

- 修复发票打印模板缺少「发票号码」字段的问题。
- 修复手机端启动页路由缺失、无法打开连接页的问题。
- 修复对账「待收 / 待付单据」不含期初欠款的问题。
- 修复财务汇总、对账单的应收应付未计入期初余额的问题。
- 修复盘点单只能建、不能删的问题。

---

## 五、升级方式

### Windows 桌面版

下载 `WMS_v3.0.0_Setup.exe` 直接安装即可。**数据库会自动升级**：新增提成相关字段与期初相关数据表，旧数据不受影响，历史单据的提成默认为 0。

> 小提示：如果之前录过提成，本次升级后部分单据的毛利会有小幅变化（提成口径已统一为「扣点后实付计入成本」），属正常现象。

### NAS / Docker 自部署

把新源码覆盖到 `wms_app/`，然后重建容器后访问即可：

```
docker compose up -d --build
```

数据库结构同样会自动升级。手机端无需重装——页面由服务器提供，服务器更新后手机端刷新即为新版。

---

## 六、安装包说明

- **下载地址**：https://github.com/kenowong/wms/releases/download/v3.0.0/WMS_v3.0.0_Setup.exe
  （发布页：https://github.com/kenowong/wms/releases/tag/v3.0.0）
- 安装包内置 **Tesseract OCR 引擎（含中文词库）**，体积约 137MB。发票识别在**完全离线**环境下可用：优先解析发票二维码，无二维码时用中文 OCR 兜底。
- 安装不需要管理员权限，可选择任意安装目录，安装完成自动创建桌面与开始菜单快捷方式。
